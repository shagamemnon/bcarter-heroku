<div id="panel" class="panel slideout-panel slideout-panel-right">
    <header class="panel-header">
      <button class="btn-hamburger js-slideout-toggle"><span>☰</span><a class="header-font">Menu</a></button>
  </header>

</div>
<header id="default-nav-header" class="nav-header nav-header-fixed animated">
    <a id="home-button" href="{{@blog.url}}/#open">
{{!--         <img src="{{asset "img/icons/icon.png"}}" alt="{{@blog.title}} icon" class="icon rounded hvr-buzz-out" /> --}}
        <img src="{{@blog.logo}}" alt="{{@blog.title}} avatar" class="hvr-buzz-out" />
      </a>
    <a class="tags-button right">
        <i class="fa fa-search hvr-grow"><span>Search Brandon's posts & videos</span></i>

    </a>
</header>
<nav id="menu" class="menu slideout-menu slideout-menu-right">
  <h3>BrandonCarter.com</h3>
    <section class="menu-section">
      <ul class="menu-section-list">
        {{#foreach navigation}}
        <li class="nav-{{slug}} {{#if current}} nav-current{{/if}}">
            <a href="{{this.url}}">{{label}}</a>
        </li>
        {{/foreach}}
        <li class="subscribe-button nav-blog hide {{#if current}} nav-current{{/if}}">
            <a class="subscribe-link" href="{{@blog.url}}/#open" title="subscribe">Subscribe</a>
        </li>
      </ul>
    </section>
</nav>
